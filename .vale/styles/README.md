# Vale styles

This directory is reserved for local Vale styles if the project later adds custom prose rules.

The current configuration relies on Vale's built-in checks and a repository-level workflow.
