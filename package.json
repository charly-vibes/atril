{
  "name": "atril",
  "version": "0.0.1",
  "description": "A music stand for your project's score — web-based readers for the wai ecosystem",
  "type": "module",
  "scripts": {
    "build": "bun run src/build.ts",
    "dev": "bun run --watch src/build.ts",
    "test": "bun test",
    "test:e2e": "bunx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/bun": "^1.3.12",
    "typescript": "^5.8.0"
  }
}
